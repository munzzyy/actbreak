on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install -r requirements.txt
      - name: Run tests
        run: pytest -v
      - run: echo done
      -
        name: Trailing dash-alone step
        run: echo trailing
